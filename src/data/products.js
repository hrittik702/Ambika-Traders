/**
 * Ambika Traders — Products Catalog Data (Live Database Mode)
 * Empty initial catalog. Real products are dynamically managed from /admin/products.
 */

export const products = [];

export default products;
