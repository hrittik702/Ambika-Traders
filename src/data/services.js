/**
 * Ambika Traders — Services Data (Live Database Mode)
 * Empty initial services. Real services are dynamically managed from /admin/services.
 */

export const services = [];

export default services;
